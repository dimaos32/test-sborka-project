mixin offers(data = [])

  section.offers
    .container
      h1.visually-hidden Список предложений
      ul.offers__list
        each offer in data
          li.offers__item
            +offer-card(offer).offers__card
